<%= form_for(@product_description_image) do |f| %>
  <% if @product_description_image.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@product_description_image.errors.count, "error") %> prohibited this product_description_image from being saved:</h2>

      <ul>
      <% @product_description_image.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :image %><br>
    <%= f.file_field :image %>
  </div>
  
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
