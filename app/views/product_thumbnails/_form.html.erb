<%= form_for(@product_thumbnail) do |f| %>
  <% if @product_thumbnail.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@product_thumbnail.errors.count, "error") %> prohibited this product_thumbnail from being saved:</h2>

      <ul>
      <% @product_thumbnail.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :image %><br>
    <%= f.file_field :image %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
